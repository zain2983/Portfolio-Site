import type { NextConfig } from "next";

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["pfpmaker.com" , "framerusercontent.com"], // Add external image domain here
  },
};
export default nextConfig;


